[package]
name = "axis-math"
version = "1.0.0-main"
edition = "2021"
license = "MPL-2.0"

[dependencies]
serde = { version = "1.0.147", features = ["derive"], optional = true }

[features]
default = ["std"]
std = []
